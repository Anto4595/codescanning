name: Display Message Workflow

on: [push]

jobs:
  display-message:
    runs-on: ubuntu-latest
    steps:
      - name: Display predefined message
        run: echo "This is a test message. Status: tested"